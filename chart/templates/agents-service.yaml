{{- if .Values.agents.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "yeti.agents.fullname" . }}
  labels:
    app.kubernetes.io/component: agents
    {{- include "yeti.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.agents.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: agents
    {{- include "yeti.selectorLabels" . | nindent 4 }}
{{- end }}
