{{- if not .Values.config.existingSecret }}
{{- $name := printf "%s-secret" (include "yeti.fullname" .) -}}
{{/* Inline values win (ArgoCD-safe, deterministic). Else preserve the existing
     Secret via lookup, else generate. NOTE: lookup returns empty during an
     ArgoCD diff/dry-run, so the random fallback CHURNS every sync — set
     config.secrets.* inline when you have no secret manager. */}}
{{- $secret := .Values.config.secrets.yetiSecret -}}
{{- $user := .Values.config.secrets.yetiUser -}}
{{- if or (empty $secret) (empty $user) -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $name -}}
{{- if empty $secret -}}
{{- $secret = randAlphaNum 48 -}}
{{- if and $existing (hasKey $existing.data "yeti-secret") -}}{{- $secret = index $existing.data "yeti-secret" | b64dec -}}{{- end -}}
{{- end -}}
{{- if empty $user -}}
{{- $user = randAlphaNum 20 -}}
{{- if and $existing (hasKey $existing.data "yeti-user") -}}{{- $user = index $existing.data "yeti-user" | b64dec -}}{{- end -}}
{{- end -}}
{{- end }}
# App secret (generated once, preserved across upgrades). Keys:
#   yeti-secret -> YETI_AUTH_SECRET_KEY
#   yeti-user   -> initial 'yeti' admin password
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  labels:
    {{- include "yeti.labels" . | nindent 4 }}
type: Opaque
stringData:
  yeti-secret: {{ $secret | quote }}
  yeti-user: {{ $user | quote }}
{{- end }}
{{- if and .Values.arangodb.enabled (not .Values.arangodb.rootPasswordExistingSecret) }}
{{- $aname := printf "%s-arango-root" (include "yeti.fullname" .) -}}
{{- $apassword := .Values.config.secrets.arangoPassword -}}
{{- if empty $apassword -}}
{{- $aexisting := lookup "v1" "Secret" .Release.Namespace $aname -}}
{{- $apassword = randAlphaNum 32 -}}
{{- if and $aexisting (hasKey $aexisting.data "password") -}}
{{- $apassword = index $aexisting.data "password" | b64dec -}}
{{- end -}}
{{- end }}
---
# Dedicated ArangoDB root secret. The kube-arangodb operator bootstrap reads
# username/password from here (and REQUIRES no other keys). Yeti reads the same
# password as YETI_ARANGODB_PASSWORD.
apiVersion: v1
kind: Secret
metadata:
  name: {{ $aname }}
  labels:
    {{- include "yeti.labels" . | nindent 4 }}
type: Opaque
stringData:
  username: "root"
  password: {{ $apassword | quote }}
{{- end }}
